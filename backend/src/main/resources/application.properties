spring.application.name=PathFinder

# env variables will be passed to AWS EC2 container directly using bash profile
spring.datasource.url=jdbc:postgresql://path-finder.czmcoew2ykts.eu-central-1.rds.amazonaws.com:5432/path-finder
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

logging.level.org.springframework.security=DEBUG
#logging.file.name=pathfinder.log

osm.graph.file=s3://path-finder-uliana/Muenchen.osm.pbf